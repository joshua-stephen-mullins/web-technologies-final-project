package team.projectpulse.security;

// Owner: Josh (Person 1)
// Configures Spring Security: JWT resource server, CORS, role-based access rules
// Related UCs: UC-25 (student login), UC-30 (instructor login), all secured endpoints
public class SecurityConfig {
    // TODO: Implement
    // - Configure JWT decoder
    // - Set up CORS for Vue frontend (localhost:5173 in dev, Azure URL in prod)
    // - Define role-based access rules per endpoint (ROLE_ADMIN, ROLE_INSTRUCTOR, ROLE_STUDENT)
    // - Permit /api/auth/**, /api/users/register/** without auth
}
