package team.projectpulse.user.resetpassword;

import org.springframework.stereotype.Service;

// Owner: Josh (Person 1)
@Service
public class PasswordResetService {
    // TODO: Implement
    // - initiateReset(email): generate token, send email
    // - completeReset(token, newPassword): validate token, update password
}
