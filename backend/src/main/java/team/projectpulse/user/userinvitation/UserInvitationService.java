package team.projectpulse.user.userinvitation;

import org.springframework.stereotype.Service;

// Owner: Josh (Person 1)
// Related UCs: UC-11 (send student invites), UC-18 (send instructor invites)
@Service
public class UserInvitationService {
    // TODO: Implement
    // - sendInvitations(emails, role, sectionId): generate unique tokens, send emails
    // - validateToken(token): return invitation or throw exception
}
