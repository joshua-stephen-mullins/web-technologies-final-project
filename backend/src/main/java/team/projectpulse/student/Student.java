package team.projectpulse.student;

// Owner: Oscar (Person 2)
// Related UCs: UC-11,12,13,15,16,17
// Note: Student is a PeerEvaluationUser with ROLE_STUDENT — this class may extend or wrap it
public class Student {
    // TODO: Implement JPA entity (or use PeerEvaluationUser with role filtering)
    // Fields: id, firstName, lastName, email
    // Relationships: team (ManyToOne), section (via team)
}
