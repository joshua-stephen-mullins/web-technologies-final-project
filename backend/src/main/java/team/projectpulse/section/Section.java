package team.projectpulse.section;

// Owner: Josh (Person 1)
// Related UCs: UC-2,3,4,5 (section CRUD), UC-6 (active weeks)
public class Section {
    // TODO: Implement JPA entity
    // Fields: id, name (unique), startDate, endDate
    // Relationships: rubric (ManyToOne), teams (OneToMany), weeks (OneToMany)
}
