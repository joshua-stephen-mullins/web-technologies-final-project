package team.projectpulse.section;

// Owner: Josh (Person 1)
// Represents a single week within a section; isActive drives WAR/eval submission windows
// Related UCs: UC-6 (active weeks setup), BR-2 (business rule for active weeks)
public class Week {
    // TODO: Implement JPA entity
    // Fields: id, section (ManyToOne), weekNumber, startDate, endDate, isActive
}
