package team.projectpulse.instructor;

// Owner: Whitey (Person 3)
// Related UCs: UC-18,19,20,21,22,23,24,30
// Note: Instructor is a PeerEvaluationUser with ROLE_INSTRUCTOR
public class Instructor {
    // TODO: Implement JPA entity (or use PeerEvaluationUser with role filtering)
    // Fields: id, firstName, lastName, email, enabled (active/deactivated)
    // Relationships: teams (ManyToMany)
}
