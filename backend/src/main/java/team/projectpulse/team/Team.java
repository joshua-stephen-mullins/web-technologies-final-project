package team.projectpulse.team;

// Owner: Oscar (Person 2)
// Related UCs: UC-7,8,9,10,13,14
public class Team {
    // TODO: Implement JPA entity
    // Fields: id, name (unique), description, websiteUrl
    // Relationships: section (ManyToOne), students (OneToMany -> PeerEvaluationUser), instructors (ManyToMany)
}
