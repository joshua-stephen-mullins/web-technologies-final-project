import axiosClient from './axiosClient'

// Owner: Josh (Person 1)
// UC-25 (register), UC-26 (edit account), UC-30 (instructor register)
export const userApi = {
  // TODO: register(token, data), getMe(), updateMe(data),
  //       forgotPassword(email), resetPassword(token, newPassword)
}
