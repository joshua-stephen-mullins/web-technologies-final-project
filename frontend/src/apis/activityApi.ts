// Owner: Oscar (Person 2) — UC-27,32
export const activityApi = {
  // TODO: getByStudentAndWeek(studentId, weekId), create(data), update(id, data), remove(id),
  //       getTeamWARReport(teamId, weekId)
}
